{
  "servers": {
    "uds-mcp": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "uds-mcp"],
      "cwd": "${workspaceFolder}",
      "env": {
        "UDS_MCP_CONFIG_PATH": "${workspaceFolder}/uds.toml"
      }
    }
  }
}